specVersion: "v1"
serviceId: "jobapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "jobapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
