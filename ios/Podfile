# Updated Podfile to set platform to iOS 12.0, enforce deployment target for pods,
# and sanitize stray standalone "-G" tokens from common build flag keys.
# (Merged with your original Podfile contents.)
# NOTE: after saving this file run `cd ios && pod install --repo-update`.

# Uncomment this line to define a global platform for your project
platform :ios, '13.0'

# CocoaPods analytics sends network stats synchronously affecting flutter build latency.
ENV['COCOAPODS_DISABLE_STATS'] = 'true'

project 'Runner', {
  'Debug' => :debug,
  'Profile' => :release,
  'Release' => :release,
}

def flutter_root
  generated_xcode_build_settings_path = File.expand_path(File.join('..', 'Flutter', 'Generated.xcconfig'), __FILE__)
  unless File.exist?(generated_xcode_build_settings_path)
    raise "#{generated_xcode_build_settings_path} must exist. If you're running pod install manually, make sure flutter pub get is executed first"
  end

  File.foreach(generated_xcode_build_settings_path) do |line|
    matches = line.match(/FLUTTER_ROOT\=(.*)/)
    return matches[1].strip if matches
  end
  raise "FLUTTER_ROOT not found in #{generated_xcode_build_settings_path}. Try deleting Generated.xcconfig, then run flutter pub get"
end

require File.expand_path(File.join('packages', 'flutter_tools', 'bin', 'podhelper'), flutter_root)

flutter_ios_podfile_setup

target 'Runner' do
  use_frameworks!

  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
  target 'RunnerTests' do
    inherit! :search_paths
  end
end

post_install do |installer|
  installer.pods_project.targets.each do |target|
    flutter_additional_ios_build_settings(target)
    target.build_configurations.each do |config|
      config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'
      keys = %w[COMPILER_FLAGS OTHER_CFLAGS OTHER_CPLUSPLUSFLAGS OTHER_LDFLAGS]
      keys.each do |k|
        v = config.build_settings[k]
        next unless v
        if v.is_a?(Array)
          new_v = v.map { |e| e.is_a?(String) ? e.gsub(/(^|\s)-G(\s|$)/, ' ').gsub(/\s+/, ' ').strip : e }
          new_v = new_v.map(&:to_s).map(&:strip).reject(&:empty?)
          config.build_settings[k] = new_v unless new_v.empty?
        else
          nv = v.to_s.gsub(/(^|\s)-G(\s|$)/, ' ').gsub(/\s+/, ' ').strip
          config.build_settings[k] = nv unless nv.empty?
        end
      end
    end
  end
  installer.aggregate_targets.each do |aggregate|
    if aggregate.respond_to?(:user_project) && aggregate.user_project
      aggregate.user_project.targets.each do |proj_target|
        proj_target.build_configurations.each do |proj_config|
          proj_config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '13.0'
        end
      end
    end
  end
end
